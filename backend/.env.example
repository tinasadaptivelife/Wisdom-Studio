# Get a free key at https://aihorde.net/register (or use the shared
# anonymous key below for testing — much lower priority in the generation
# queue, so expect slower results).
AI_HORDE_API_KEY=0000000000
